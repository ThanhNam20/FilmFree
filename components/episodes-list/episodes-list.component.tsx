import { View, Text, FlatList, Button } from "react-native";
import React from "react";

const EpisodesListComponent = ({ episodeList }: any) => {
  const { episodeListData } = episodeList;
  return (
    <View>

    </View>
  );
};

export default EpisodesListComponent;
