import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  text: {
    color: "#fff",
    paddingLeft: 10,
    marginBottom: 10,
    marginTop: 15,
    fontWeight: 'bold',
    
  }
});

export const CategoryStyle = styles;